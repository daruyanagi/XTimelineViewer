# イシュー解決前にコメントをすべて読む

イシューを実装・解決する前に、必ず `gh issue view <number> --comments` でコメントまで全部読むこと。

本文だけでなくコメントに追加要件や修正方針が書かれている場合があり、読まずに着手すると対応漏れが発生する。

## 手順

1. `gh issue view <number> --comments` でイシュー全文＋コメントを確認
2. コメントの要件をすべて把握してから実装を開始する
